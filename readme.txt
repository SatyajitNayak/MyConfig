This is an application to mimic a configuration setting for an application. The front end would be written in Angular7 and the backend would use Spring Rest with Hibernate. Currently planing to use in memory database like hsql. 

We might end up using spring boot for the backend configuration. 
